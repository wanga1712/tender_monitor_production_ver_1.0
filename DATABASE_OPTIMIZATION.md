# Оптимизация базы данных TenderMonitor

## 📋 Описание

Скрипт `database_work/database_optimizer.py` анализирует структуру базы данных и предлагает оптимизации для улучшения производительности.

## 🔍 Что проверяется

1. **Индексы** - проверяет наличие индексов на колонках, используемых в WHERE запросах
2. **Внешние ключи** - проверяет наличие внешних ключей для целостности данных
3. **Уникальные ограничения** - проверяет уникальность важных полей (ИНН, номера контрактов)

## 🚀 Использование

### Вариант 1: Запуск через analyze_db.py

```bash
python analyze_db.py
```

Это создаст файл `database_optimization.sql` с SQL командами для оптимизации.

### Вариант 2: Прямое использование

```python
from database_work.database_optimizer import DatabaseOptimizer

optimizer = DatabaseOptimizer()
try:
    # Анализ без применения (dry run)
    script = optimizer.apply_optimizations(dry_run=True)
    
    # Применить оптимизации
    # optimizer.apply_optimizations(dry_run=False)
finally:
    optimizer.close()
```

## 📊 Что оптимизируется

### Индексы (высокий приоритет)

Создаются индексы на колонках, которые часто используются в WHERE запросах:

- `customer.customer_inn` - поиск заказчиков по ИНН
- `contractor.inn` - поиск подрядчиков по ИНН
- `reestr_contract_44_fz.contract_number` - поиск контрактов 44-ФЗ
- `reestr_contract_223_fz.contract_number` - поиск контрактов 223-ФЗ
- `file_names_xml.file_name` - проверка обработанных файлов
- `collection_codes_okpd.code` и `sub_code` - поиск ОКПД кодов
- `region.code` - поиск регионов
- `trading_platform.trading_platform_name` - поиск торговых площадок
- `links_documentation_44_fz.link` и `links_documentation_223_fz.link` - поиск ссылок

### Внешние ключи (средний приоритет)

Создаются внешние ключи для обеспечения целостности данных:

- `customer.region_id` → `region(id)`
- `reestr_contract_44_fz.customer_id` → `customer(id)`
- `reestr_contract_44_fz.contractor_id` → `contractor(id)`
- `reestr_contract_44_fz.region_id` → `region(id)`
- `reestr_contract_44_fz.okpd_id` → `collection_codes_okpd(id)`
- `reestr_contract_44_fz.trading_platform_id` → `trading_platform(id)`
- Аналогично для `reestr_contract_223_fz`
- `links_documentation_44_fz.contract_id` → `reestr_contract_44_fz(id)`
- `links_documentation_223_fz.contract_id` → `reestr_contract_223_fz(id)`

### Уникальные ограничения (высокий приоритет)

Создаются уникальные индексы для предотвращения дубликатов:

- `customer.customer_inn` - уникальный ИНН заказчика
- `contractor.inn` - уникальный ИНН подрядчика
- `reestr_contract_44_fz.contract_number` - уникальный номер контракта
- `reestr_contract_223_fz.contract_number` - уникальный номер контракта
- `file_names_xml.file_name` - уникальное имя файла
- `region.code` - уникальный код региона

## ⚡ Ожидаемое улучшение производительности

После применения оптимизаций:

- **Поиск по ИНН**: ускорение в 10-100 раз (зависит от размера таблицы)
- **Поиск контрактов**: ускорение в 5-50 раз
- **Проверка файлов**: ускорение в 20-200 раз
- **JOIN операции**: ускорение за счет индексов на внешних ключах
- **Предотвращение дубликатов**: автоматическая проверка на уровне БД

## ⚠️ Важные замечания

1. **Перед применением** обязательно сделайте резервную копию БД
2. **Проверьте скрипт** `database_optimization.sql` перед выполнением
3. **Внешние ключи** могут замедлить вставку данных, но улучшат целостность
4. **Индексы** занимают место на диске, но значительно ускоряют поиск

## 🔧 Ручное применение

Если хотите применить оптимизации вручную:

```bash
psql -U your_user -d your_database -f database_optimization.sql
```

Или через pgAdmin: откройте файл `database_optimization.sql` и выполните его.

## 📈 Мониторинг производительности

После применения оптимизаций можно проверить использование индексов:

```sql
-- Проверка использования индексов
SELECT 
    schemaname,
    tablename,
    indexname,
    idx_scan as index_scans,
    idx_tup_read as tuples_read,
    idx_tup_fetch as tuples_fetched
FROM pg_stat_user_indexes
WHERE schemaname = 'public'
ORDER BY idx_scan DESC;
```

## 🐛 Устранение проблем

Если при применении оптимизаций возникают ошибки:

1. Проверьте, что все таблицы существуют
2. Убедитесь, что нет дубликатов данных в колонках, которые должны быть уникальными
3. Проверьте, что все внешние ключи ссылаются на существующие записи
4. При необходимости очистите данные перед применением ограничений

